#### 例（リファクタリング前）
```
$discountedItems =
	array_filter($items, fn($item) => $item->isDiscounted); 
return $discountedItems;
```

↓

#### 例（リファクタリング後）
```
return array_filter($items, fn($item) => $item->isDiscounted);
```